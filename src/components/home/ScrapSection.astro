---
import { SITE_CONFIG } from "../../data/site-config";
import Container from "../ui/Container.astro";
import { AlertTriangle, Phone } from "lucide-react";
---

<section class="py-16 bg-white">
  <Container>
    <div
      class="relative rounded-3xl overflow-hidden shadow-2xl shadow-emerald-900/20"
    >
      {/* Gradient Background */}
      <div
        class="absolute inset-0 bg-gradient-to-r from-emerald-900 to-emerald-800"
      >
      </div>

      {/* Pattern Overlay */}
      <div class="absolute inset-0 opacity-10 bg-pattern-islamic"></div>

      <div
        class="relative z-10 p-8 md:p-16 flex flex-col md:flex-row items-center justify-between gap-10 text-white"
      >
        <div class="flex-1 text-center md:text-right">
          <div
            class="inline-flex items-center gap-2 rounded-full bg-white/10 backdrop-blur-md border border-white/20 px-4 py-1 text-sm font-bold text-emerald-100 mb-6"
          >
            <AlertTriangle className="h-4 w-4 text-amber-400" />
            <span>خدمة خاصة</span>
          </div>

          <h2 class="mb-6 text-3xl md:text-5xl font-bold leading-tight">
            نشتري السكراب والمكيفات الخربانة
          </h2>

          <p
            class="text-lg md:text-xl text-emerald-100 leading-relaxed mb-8 max-w-2xl"
          >
            لا تشيل هم النقل والتنظيف. نشتري جميع أنواع السكراب، الحديد، النحاس،
            والمكيفات التالفة بأسعار عادلة ونقوم بالنقل فوراً.
          </p>

          <ul
            class="flex flex-wrap gap-4 justify-center md:justify-start text-sm font-bold text-emerald-200"
          >
            <li class="flex items-center gap-2">
              <div class="w-2 h-2 rounded-full bg-amber-400"></div>
              مكيفات شباك وسبليت
            </li>
            <li class="flex items-center gap-2">
              <div class="w-2 h-2 rounded-full bg-amber-400"></div>
              أجهزة كهربائية تالفة
            </li>
            <li class="flex items-center gap-2">
              <div class="w-2 h-2 rounded-full bg-amber-400"></div>
              مطابخ المنيوم
            </li>
          </ul>
        </div>

        <div class="flex-shrink-0">
          <a
            href={`tel:${SITE_CONFIG.phone}`}
            class="track-call group relative inline-flex items-center gap-3 rounded-full bg-white px-8 py-4 text-xl font-bold text-emerald-900 transition-all hover:scale-105 hover:shadow-xl hover:shadow-white/20"
          >
            <span
              class="absolute inset-0 rounded-full bg-gradient-to-r from-amber-400 to-amber-500 opacity-0 transition-opacity group-hover:opacity-100 -z-10 blur-md"
            ></span>
            <Phone className="h-6 w-6" />
            <span>بيع السكراب الآن</span>
          </a>
        </div>
      </div>
    </div>
  </Container>
</section>
