---
import Container from "../ui/Container.astro";
import { Image } from "astro:assets";
import { MessageCircle, Phone, TrendingUp, MapPin } from "lucide-react";
import { SITE_CONFIG } from "../../data/site-config";

// Authentic Saudi/Modern Living Room
const HERO_IMAGE =
  "https://images.unsplash.com/photo-1556228453-efd6c1ff04f6?q=80&w=2070&auto=format&fit=crop";
---

<section
  class="relative min-h-[90vh] flex items-center justify-center overflow-hidden"
>
  {/* Background Image */}
  <div class="absolute inset-0 z-0">
    <Image
      src={HERO_IMAGE}
      alt="Luxury Saudi Home"
      width={2070}
      height={1380}
      loading="eager"
      fetchpriority="high"
      class="w-full h-full object-cover grayscale-[20%] contrast-125"
    />
    {/* Gradient Overlay - Radial for depth */}
    <div
      class="absolute inset-0 bg-gradient-to-t from-emerald-950 via-emerald-950/80 to-transparent"
    >
    </div>
    <div
      class="absolute inset-0 bg-radial-[circle_at_center,_var(--tw-gradient-stops)] from-transparent via-emerald-950/50 to-emerald-950/90"
    >
    </div>
  </div>

  <Container className="relative z-10 text-center pt-20">
    {/* Floating 3D Element (Icon for now) */}
    <div
      class="absolute top-10 right-10 lg:right-20 animate-float hidden lg:block opacity-80"
    >
      <div
        class="w-24 h-24 bg-gradient-to-br from-amber-300 to-amber-600 rounded-2xl rotate-12 shadow-2xl flex items-center justify-center border border-white/20 backdrop-blur-sm"
      >
        <span class="text-5xl">💰</span>
      </div>
    </div>

    {/* Badge */}
    <div
      class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-black/30 backdrop-blur-md border border-brand-accent/30 mb-8 animate-fade-up"
    >
      <span class="w-2 h-2 rounded-full bg-brand-accent animate-pulse"></span>
      <span class="text-brand-accent text-sm font-bold tracking-wide"
        >الأعلى تقييماً في الرياض 2025</span
      >
    </div>

    {/* Main Heading */}
    <h1
      class="mb-6 text-5xl md:text-7xl font-bold leading-tight text-white drop-shadow-xl animate-fade-up [animation-delay:200ms]"
    >
      بيع أثاثك في <span
        class="text-brand-accent underline decoration-brand-accent/30 underline-offset-8"
        >60 دقيقة</span
      >
      <br />
      <span
        class="text-transparent bg-clip-text bg-gradient-to-r from-brand-primary to-brand-accent"
      >
        كاش فوري في الرياض
      </span>
    </h1>

    <p
      class="mb-10 text-xl md:text-2xl text-slate-200 leading-relaxed max-w-3xl mx-auto animate-fade-up [animation-delay:400ms]"
    >
      نحول أثاثك المستعمل إلى كاش فوري. نشتري المكيفات، غرف النوم، والمطابخ
      بأعلى الأسعار.
    </p>

    {/* Buttons */}
    <div
      class="flex flex-col sm:flex-row items-center justify-center gap-6 animate-fade-up [animation-delay:600ms]"
    >
      <a
        href={`https://wa.me/${SITE_CONFIG.whatsapp}`}
        aria-label="تواصل معنا عبر واتساب"
        class="track-whatsapp group relative flex items-center justify-center gap-3 px-8 py-4 rounded-full bg-gradient-to-r from-brand-primary to-emerald-600 text-white text-lg font-bold shadow-lg shadow-brand-primary/30 overflow-hidden transition-transform hover:scale-105"
      >
        <div class="absolute inset-0 bg-white/20 animate-shimmer"></div>
        <MessageCircle className="w-6 h-6" />
        <span>اطلب تسعير واتساب</span>
      </a>

      <a
        href={`tel:${SITE_CONFIG.phone}`}
        aria-label="اتصل بنا الآن"
        class="track-call flex items-center justify-center gap-3 px-8 py-4 rounded-full glass-card text-white text-lg font-bold hover:bg-white/20 transition-all hover:scale-105 animate-pulse-glow"
      >
        <Phone className="w-6 h-6" />
        <span>اتصل الآن: {SITE_CONFIG.phone}</span>
      </a>
    </div>

    {/* Floating Ticker */}
    <div
      class="mt-16 inline-flex items-center gap-6 px-6 py-3 rounded-full bg-black/40 backdrop-blur-md border border-white/10 text-white/90 text-sm animate-fade-up [animation-delay:800ms]"
    >
      <div class="flex items-center gap-2">
        <TrendingUp className="w-4 h-4 text-brand-accent" />
        <span>15,000+ قطعة تم شراؤها</span>
      </div>
      <div class="w-px h-4 bg-white/20"></div>
      <div class="flex items-center gap-2">
        <MapPin className="w-4 h-4 text-brand-primary" />
        <span>نشط في 120 حي بالرياض</span>
      </div>
      <div class="w-px h-4 bg-white/20"></div>
      <div class="hidden sm:flex items-center gap-2">
        <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
        <span>شاحناتنا في شمال الرياض الآن</span>
      </div>
    </div>
  </Container>
</section>
