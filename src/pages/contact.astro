---
import Layout from "../layouts/Layout.astro";
import Container from "../components/ui/Container.astro";
import MapEmbed from "../components/ui/MapEmbed.astro";
import { Phone, Mail, MapPin, Clock, MessageCircle } from "lucide-react";
import { SITE_CONFIG } from "../data/site-config";

const title = "اتصل بنا | شركة العمدة لشراء الاثاث المستعمل";
const description =
  "تواصل مع شركة العمدة لبيع اثاثك المستعمل في الرياض. خدمة عملاء على مدار 24 ساعة.";
---

<Layout title={title} description={description}>
  {/* Page Header */}
  <section class="bg-slate-900 py-20 relative overflow-hidden">
    <div
      class="absolute inset-0 bg-gradient-to-br from-emerald-900/40 to-slate-900/90 z-10"
    >
    </div>
    <div
      class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/arabesque.png')] opacity-10 animate-pulse-slow"
    >
    </div>
    <Container className="relative z-20 text-center">
      <h1
        class="text-4xl md:text-6xl font-bold mb-6 text-white animate-fade-in-up"
      >
        تواصل{" "}
        <span
          class="text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 to-emerald-200"
        >
          معنا
        </span>
      </h1>
      <p
        class="text-xl text-slate-300 max-w-2xl mx-auto animate-fade-in-up delay-100"
      >
        نحن هنا لخدمتك. تواصل معنا للحصول على أفضل عرض سعر لاثاثك المستعمل.
      </p>
    </Container>
  </section>

  {/* Quick Action Section (Prioritized WhatsApp) */}
  <section class="py-12 bg-white border-b border-slate-100">
    <Container>
      <div class="max-w-3xl mx-auto text-center space-y-8">
        <h2 class="text-2xl font-bold text-slate-900">أسرع طريقة للبيع؟</h2>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href={`https://wa.me/${SITE_CONFIG.whatsapp}`}
            class="group relative flex items-center justify-center gap-3 px-8 py-5 rounded-2xl bg-[#25D366] text-white text-xl font-bold shadow-xl shadow-emerald-900/10 hover:shadow-emerald-900/20 transition-all duration-300 hover:-translate-y-1 overflow-hidden"
          >
            <div
              class="absolute inset-0 w-full h-full bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:animate-[shimmer_1.5s_infinite]"
            >
            </div>
            <MessageCircle className="w-8 h-8" />
            <span>أرسل الصور واتساب (رد فوري)</span>
          </a>
          <a
            href={`tel:${SITE_CONFIG.phone}`}
            class="flex items-center justify-center gap-3 px-8 py-5 rounded-2xl bg-slate-100 text-slate-700 text-xl font-bold hover:bg-slate-200 transition-all duration-300"
          >
            <Phone className="w-6 h-6" />
            <span>اتصال هاتفي</span>
          </a>
        </div>
        <p class="text-slate-500 text-sm">
          متاحون 24 ساعة للرد على استفساراتكم
        </p>
      </div>
    </Container>
  </section>

  <section class="py-20 bg-slate-50">
    <Container>
      <div class="grid lg:grid-cols-2 gap-12">
        {/* Contact Info */}
        <div class="space-y-8 animate-fade-in-right">
          <div
            class="bg-white p-8 rounded-3xl shadow-sm border border-slate-100"
          >
            <h2
              class="text-2xl font-bold text-slate-900 mb-8 flex items-center gap-3"
            >
              <span class="w-2 h-8 bg-emerald-500 rounded-full"></span>
              معلومات الاتصال
            </h2>
            <div class="space-y-6">
              <a
                href={`tel:${SITE_CONFIG.phone}`}
                class="flex items-start gap-4 group p-4 rounded-2xl hover:bg-emerald-50 transition-colors duration-300"
              >
                <div
                  class="w-12 h-12 bg-emerald-100 rounded-full flex items-center justify-center text-emerald-600 group-hover:bg-emerald-600 group-hover:text-white transition-colors duration-300 shadow-sm"
                >
                  <Phone className="w-6 h-6" />
                </div>
                <div>
                  <h3 class="font-bold text-slate-900 mb-1">رقم الهاتف</h3>
                  <p class="text-slate-600 font-mono text-lg" dir="ltr">
                    {SITE_CONFIG.phone}
                  </p>
                </div>
              </a>

              <div
                class="flex items-start gap-4 p-4 rounded-2xl hover:bg-slate-50 transition-colors duration-300"
              >
                <div
                  class="w-12 h-12 bg-emerald-100 rounded-full flex items-center justify-center text-emerald-600 shadow-sm"
                >
                  <Mail className="w-6 h-6" />
                </div>
                <div>
                  <h3 class="font-bold text-slate-900 mb-1">
                    البريد الإلكتروني
                  </h3>
                  <p class="text-slate-600">info@alomda-sa.com</p>
                </div>
              </div>

              <div
                class="flex items-start gap-4 p-4 rounded-2xl hover:bg-slate-50 transition-colors duration-300"
              >
                <div
                  class="w-12 h-12 bg-emerald-100 rounded-full flex items-center justify-center text-emerald-600 shadow-sm"
                >
                  <MapPin className="w-6 h-6" />
                </div>
                <div>
                  <h3 class="font-bold text-slate-900 mb-1">الموقع</h3>
                  <p class="text-slate-600">الرياض، المملكة العربية السعودية</p>
                </div>
              </div>

              <div
                class="flex items-start gap-4 p-4 rounded-2xl hover:bg-slate-50 transition-colors duration-300"
              >
                <div
                  class="w-12 h-12 bg-emerald-100 rounded-full flex items-center justify-center text-emerald-600 shadow-sm"
                >
                  <Clock className="w-6 h-6" />
                </div>
                <div>
                  <h3 class="font-bold text-slate-900 mb-1">ساعات العمل</h3>
                  <p class="text-slate-600">
                    نعمل على مدار 24 ساعة طوال أيام الأسبوع
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Contact Form */}
        <div
          class="bg-white p-8 lg:p-10 rounded-3xl shadow-xl border border-slate-100 animate-fade-in-left"
        >
          <h2
            class="text-2xl font-bold text-slate-900 mb-8 flex items-center gap-3"
          >
            <span class="w-2 h-8 bg-emerald-500 rounded-full"></span>
            أرسل لنا رسالة
          </h2>
          <form class="space-y-6">
            <div class="grid md:grid-cols-2 gap-6">
              <div class="group">
                <label
                  class="block text-sm font-bold text-slate-700 mb-2 group-focus-within:text-emerald-600 transition-colors"
                >
                  الاسم
                </label>
                <input
                  type="text"
                  class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/10 outline-none transition-all duration-300 bg-slate-50 focus:bg-white"
                  placeholder="اسمك الكريم"
                />
              </div>
              <div class="group">
                <label
                  class="block text-sm font-bold text-slate-700 mb-2 group-focus-within:text-emerald-600 transition-colors"
                >
                  رقم الجوال
                </label>
                <input
                  type="tel"
                  class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/10 outline-none transition-all duration-300 bg-slate-50 focus:bg-white"
                  placeholder="05xxxxxxxx"
                />
              </div>
            </div>
            <div class="group">
              <label
                class="block text-sm font-bold text-slate-700 mb-2 group-focus-within:text-emerald-600 transition-colors"
              >
                نوع الخدمة
              </label>
              <select
                class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/10 outline-none transition-all duration-300 bg-slate-50 focus:bg-white appearance-none"
              >
                <option>شراء مكيفات</option>
                <option>شراء غرف نوم</option>
                <option>شراء مطابخ</option>
                <option>شراء اجهزة كهربائية</option>
                <option>اخرى</option>
              </select>
            </div>
            <div class="group">
              <label
                class="block text-sm font-bold text-slate-700 mb-2 group-focus-within:text-emerald-600 transition-colors"
              >
                صف الأثاث (أو أرسل صورة على الواتساب)
              </label>
              <textarea
                rows="4"
                class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/10 outline-none transition-all duration-300 bg-slate-50 focus:bg-white"
                placeholder="تفاصيل طلبك..."></textarea>
            </div>
            <button
              type="submit"
              class="w-full bg-gradient-to-r from-emerald-600 to-emerald-500 hover:from-emerald-700 hover:to-emerald-600 text-white font-bold py-4 rounded-xl transition-all duration-300 shadow-lg shadow-emerald-500/30 hover:shadow-emerald-500/50 hover:-translate-y-0.5 transform"
            >
              إرسال الطلب
            </button>
          </form>
        </div>
      </div>
    </Container>
  </section>

  {/* Full Width Map Section */}
  <section class="py-16 bg-white border-t border-slate-200">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center mb-10">
        <h2 class="text-2xl font-bold text-slate-900 mb-4">
          موقعنا على الخريطة
        </h2>
        <p class="text-slate-600">
          نخدم جميع أحياء الرياض ونصل إليك أينما كنت.
        </p>
      </div>
      <div
        class="h-[500px] rounded-3xl overflow-hidden shadow-2xl shadow-emerald-900/10 border-4 border-slate-50"
      >
        <MapEmbed query="Riyadh, Saudi Arabia" />
      </div>
    </div>
  </section>
</Layout>;

